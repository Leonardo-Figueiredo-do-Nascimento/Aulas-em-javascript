//window.alert("Isso é um alerta");

//window.confirm("Isso é uma confirmação");

//prompt("Isso é um aviso.Insira um valor:");

let alerta = window.alert("Isso é um alerta");

let confirmação = window.confirm("Isso é uma confirmação");

let aviso = prompt("Isso é um aviso.Insira um valor:");